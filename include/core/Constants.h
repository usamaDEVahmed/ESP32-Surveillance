#ifndef CONSTANTS_H
#define CONSTANTS_H
#include <stdint.h>

constexpr uint16_t HTTP_PORT = 80;

constexpr uint32_t SERIAL_BAUD = 115200;

constexpr uint8_t DEFAULT_JPEG_QUALITY = 12;

constexpr uint8_t DEFAULT_BRIGHTNESS = 0;

constexpr uint8_t DEFAULT_CONTRAST = 0;

constexpr uint8_t DEFAULT_SATURATION = 0;

constexpr uint8_t DEFAULT_TARGET_FPS = 15;

constexpr uint8_t DEFAULT_MAX_CLIENTS = 5;

#endif